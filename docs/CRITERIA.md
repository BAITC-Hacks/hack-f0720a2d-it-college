# Соответствие критериям кейса

Основание: [CASE.md](CASE.md), раздел 9. Веса — из кейса, **не выставленная оценка**. Исходное состояние: [AUDIT.md](AUDIT.md). Подготовленный пример: [DEMO.json](DEMO.json), инструкция — [README](../README.md), раздел 10.

| Критерий | Вес | Реализация / статус | Где в коде | Как проверить за минуту |
|---|---:|---|---|---|
| Геймификация бизнеса | 25 | Работает: 0–100 за подтверждённые поля, 7 показателей, отдельный прогноз, подсказки, уровни, пересчёт, позиция каталога | `app/modules/rating/service.py`, `tasks/service.py`, `tasks/models.py:TaskVerification`; `static/js/helpers.js`, `constructor.js` | В полной подтверждённой карточке заменить `data`: 100→80; подтвердить: 80→100. Сравнить 7 строк и порядок каталога. Тесты `test_rating.py`, `test_confirmation.py`. Формула по длине, не семантическая. |
| Сквозной сценарий | 20 | Работает: 8 шагов, включая однократные баллы выбранной команде после подтверждённого результата | `app/modules/tasks/{router,service}.py`, `proposals/{router,service}.py`; `constructor.js`, `task.js`, `proposals.js` | `python -m pytest tests/test_demo.py -q -p no:cacheprovider` — весь путь с точными баллами; живой пример в README. Два отклика решаются вручную. |
| Качество карточки | 15 | Работает с ограничением локального режима: поля редактируются, вопросы по отсутствующим сведениям, минимум 3, ручное подтверждение | `app/modules/ai/{schemas,prompts,service}.py`, `tasks/schemas.py`; `static/js/constructor.js` | Сравнить вопросы для «Нужен сервис» и строк `Данные: ...`, `Контакт: ...`: указанные метками сведения не считаются отсутствующими. Неизвестное поле ответа →422. Заглушка не понимает свободный текст семантически. |
| Каталог и отклики | 15 | Работает для любого числа разных команд: все опубликованные задачи, включая низкие, фильтры и ручные решения. Повтор одной команды ограничен | `app/modules/catalog/{router,service}.py`, `tasks/service.py:list_published`, `proposals/service.py`; `static/js/catalog.js`, `proposals.js` | `GET /api/catalog?level=draft`: карточка №8 с 37,5. `tests/test_proposals.py`: 12 команд без общей квоты, принять несколько — остальные pending; чужому владельцу 403. Одна команда не может подать второй вариант на ту же задачу. |
| AI функция | 10 | Локальный путь работает; внешний клиент проверен только подменённым HTTP, живой провайдер не проверялся | `app/modules/ai/prompts.py`, `schemas.py`, `client.py`, `service.py`; `tests/test_ai_validation.py` | Без ключа собрать карточку. `python -m pytest tests/test_ai.py tests/test_ai_validation.py -q -p no:cacheprovider`: JSON/схема, выдуманные сведения, тайм-аут/отказ, один повтор→заглушка. |
| Техническое качество | 10 | Проверены установка, один сервер API+статики, русские ошибки, модульные границы, тесты, совместимость БД | `start.py`, `start.bat`, `app/main.py`, `db.py`, `modules/*`, `tests/*`, README/AGENTS | В отдельной копии `start.bat --check`, затем `start.bat --no-browser --port 18793`; `/health`→ok, `/`→UI. Production-авторизации, нагрузочного теста и мигратора нет. |
| Демонстрация | 5 | Подготовленный пример и все переходы проверены в браузере; длительность рассказа на сцене не измерялась | `docs/DEMO.json`, README§10, `tests/test_demo.py`, `app/seed/data/*` | 0→5→100, публикация, отклик, решение, подтверждённый результат→10 команде. 5 черновиков разной полноты и по 5 карточек/команд/откликов загружаются одной командой. Для рассказа ≤5 минут заранее открыть тексты. |

Сокращённые Python-пути модулей в таблице относительно `app/modules/`; JS-пути относительно `static/js/`. Сумма весов: 100.

## Проверка запретов

| Правило | Подтверждение |
|---|---|
| Нет автоматического назначения | Новые отклики pending; только владелец меняет выбранный отклик; принятие двух команд не меняет другие |
| Низкий рейтинг не скрывается | Нет нижнего порога каталога/отклика; UI показывает «Требует уточнения». Порог 40 — только у рекомендаций |
| Публикация после ручного подтверждения | Требуется confirmed; правка перед публикацией сбрасывает его; устаревшая серверная версия отклоняется |
| AI не дописывает факты | Выход проверяется против дословного ввода/ответа того же поля, иначе повтор/заглушка; смысл проверяет человек |
| Баллы только за подтверждённое заполнение | Снимок конкретных значений; новая/изменённая строка получает 0 до подтверждения; прогноз не влияет на каталог |

## Границы проверки

| Повторная проверка после исправлений | Фактический результат |
|---|---|
| Новая чистая копия, `start.bat --check` | Созданы окружение/БД, зависимости установлены, seed 7 пользователей / 10 задач / 5 команд / 5 откликов; **89 passed**, 1 DeprecationWarning Starlette/AnyIO |
| `start.bat --no-browser --port 18793` | Один Uvicorn, `/health` и статика доступны, повторный seed не изменил записи |
| Отдельная ручная установка по README | Новая venv, pip install, seed, **89 passed**; сервер на отдельном порту 18794 |
| Финальный браузерный сценарий на 18793 | 0→5→0 (прогноз100)→100→публикация; два отклика, принять/отклонить; результат0→10; reload сохраняет баллы |
| Проверка интерфейса | Правильные URL/title и содержимое, нет пустого экрана/оверлея ошибок, консоль без ошибок/предупреждений; 7 строк рейтинга, скриншоты desktop/mobile, низкий уровень виден |
| Репозиторий | `git diff --check` без замечаний; синтаксис 38 Python-файлов модулей проверен; нарушений импорта чужих моделей не найдено |

Ограничения проверочной среды: sandbox запрещал ensurepip/создание части временных файлов pytest (WinError 5). Повтор тех же команд с доступом к собственным временным каталогам прошёл. Это не скрытое изменение инструкции или приложения. Удалены только четыре пустых каталога временного кэша, созданных проверкой; пользовательские данные не удалялись.

- Проверки выполнены 23.09.2026 в отдельных копиях, с Python 3.11 на Windows и отдельной SQLite. Рабочие серверы/БД команды не заменялись.
- Браузер: Edge/Playwright, 1440×1000 и 390×844, полный путь включая accept/reject и результат, сохранение после reload, фильтр низкого рейтинга. Ошибок JavaScript/консоли при прогоне нет. Browser-плагин недоступен, использован установленный Playwright. Новый дизайн не создавался.
- Обычный pytest не вызывает внешний AI даже с локальным ключом. Реальный API, Linux/macOS, публичный хостинг и нагрузка не проверялись.
- Сохранена уникальность task/team. Если требовать несколько альтернатив одной команды на одну задачу, это частичное соответствие; общей квоты разных команд нет.
- Результат этапа подтверждается человеком; ссылки и отчёт автоматически не проверяются на достоверность. Это не система доказательства выполненной работы.
- Проверка доступных 3 коммитов и 101 текстового объекта истории, текущих текстовых файлов по сигнатурам секретов не нашла совпадений; `.env` не отслеживается. Это не гарантия отсутствия любых секретов.
- Рейтинг по длине, локальный шаблонный анализ и вход без пароля — ограничения MVP, а не полноценная AI-оценка/авторизация.

Публичной развёрнутой версии нет; адрес в README локальный.
